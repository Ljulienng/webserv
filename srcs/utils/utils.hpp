#ifndef UTILS_HPP
# define UTILS_HPP

#include "webserv.hpp"

/* define not member functions -> toolbox */
namespace utils {

    std::string    getTimestamp();
    std::string    myItoa(size_t number);
}

#endif